upstream django {
    server unix:///var/run/backend.sock;
}

server {
    listen 80 default_server;
    charset utf-8;
    client_max_body_size 5M;

    location / {
        uwsgi_pass django;
        include uwsgi_params;
    }
}
